# Terraform Reference Skeletons

These files are reference only, non-deploying architecture examples. No Azure resources are provisioned by this
repository.

They are intentionally incomplete and do not configure provider credentials, remote backend state,
or execution instructions. CI only reads these files through static safety validation.
