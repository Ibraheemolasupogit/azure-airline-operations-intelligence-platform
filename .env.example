# Optional local overrides for future milestones.
# Do not place real Azure credentials, keys, tenant IDs, subscription IDs, or connection strings here.
AIRLINE_OPS_ENVIRONMENT=local
AIRLINE_OPS_CONFIG=configs/platform.yaml
